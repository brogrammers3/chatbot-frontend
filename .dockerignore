# Dependencias y artefactos de build — se instalan/generan dentro de la imagen
node_modules
.next
out
build
coverage

# Git y metadatos
.git
.gitignore

# Archivos de Docker (no hace falta meterlos en el contexto)
Dockerfile*
docker-compose*
.dockerignore

# Variables de entorno — se inyectan en runtime vía env_file, nunca se hornean
.env*
!.env.example

# Misc
.DS_Store
*.pem
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*
*.tsbuildinfo
next-env.d.ts
README.md
